---
layout: page
title: All posts
---

<h1>{{ title }}</h1>

{%- for post in collections.post | reverse -%}
  <section>
    <date datetime="{{ post.date }}">{{ post.date.toLocaleDateString() }}</date>
    <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
    {% if post.data.description %}
      <p>{{ post.data.description }}</p>
    {% endif %}
  </section>
{%- endfor -%}
