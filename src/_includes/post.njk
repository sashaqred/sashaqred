---
layout: page
tags: post
---


<div class="post">
  <h1>{{ title }}</h1>
  <span>
    <date datetime="{{ page.date }}">{{ page.date.toLocaleDateString() }}</date>
    •
    <span>{{ content | readingTime }}</span>
  </span>
  <p>{{ description }}</p>
  {{ content | toc | safe }}
  {{ content | safe }}
</div>
