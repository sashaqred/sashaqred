---
layout: page
tags: post
permalink: /{{ lang }}/blog/{{ page.date | date:"u-MM-d") }}-{{ page.fileSlug }}/
---


<div class="post">
  <h1>{{ title }}</h1>
  <span>
    <time datetime="{{ page.date }}">{{ page.date | date }}</time>
    •
    <span>{{ content | readingTime }}</span>
  </span>
  {% if canonical %}
    / Originaly posted at <a href="{{canonical.url}}">{{canonical.name}}</a>
  {% endif %}
  {% if description %}
    <p>{{ description | safe }}</p>
  {% endif %}
  {{ content | toc | safe }}
  {{ content | safe }}
</div>
