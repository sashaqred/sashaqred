---
layout: page
tags: post
permalink: /blog/{{ page.date | date:"u-MM-d") }}-{{ page.fileSlug }}/
---


<div class="post">
  <h1>{{ title }}</h1>
  <span>
    <time datetime="{{ page.date }}">{{ page.date | date }}</time>
    •
    <span>{{ content | readingTime }}</span>
  </span>
  <p>{{ description | safe }}</p>
  {{ content | toc | safe }}
  {{ content | safe }}
</div>
